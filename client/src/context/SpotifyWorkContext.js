/* Global state which store information about the User's Work Playlist, 
including the Playlist Songs and id. State is used throughout the Spotify Work 
section of Application.
*/

import { createContext } from "react";

export const SpotifyWorkContext = createContext(null);
