/* ChillContext which is a global piece of state. State is specifically used by 
the Spotify Section of site and also the CountdownTimer
*/

import { createContext } from "react";

export const ChillContext = createContext(null);
