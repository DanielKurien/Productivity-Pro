/* Global state needed throughout the spotify section of the application.
The token state is used to Spotify for Spotify Data of user.
*/
import { createContext } from "react";

export const TokenContext = createContext(null);
