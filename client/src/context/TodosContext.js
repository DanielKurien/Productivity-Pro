/*Global State which stores the todos of the user. This state is
needed for the TodoSection of the application and Calendar Section
*/

import { createContext } from "react";

export const TodosContext = createContext(null);
